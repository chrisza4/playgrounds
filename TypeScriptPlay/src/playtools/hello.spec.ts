/// <reference path="../../typings/index.d.ts" />

import { expect } from 'chai'
import { plus } from './hello';

describe('User Model Unit Tests:', () => {
  it('should be sane', () => {
    expect(1).to.equal(1)
  })
})